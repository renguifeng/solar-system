version: '3.8'

services:
  solar-system:
    build: .
    container_name: solar-system
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src
      - ./index.html:/app/index.html
    restart: unless-stopped
